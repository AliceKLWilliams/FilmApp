<% include ../partials/header %>

<div class="container">
    <div class="user">
        <h3><%=user.username%></h3>
    </div>
    <div class="watched">
        <% watched.forEach(film => { %>
            <h4><%= film.title %></h4>
            <h5><%= film.year %></h5>
        <% }) %>
    </div>
</div>

<% include ../partials/footer %>